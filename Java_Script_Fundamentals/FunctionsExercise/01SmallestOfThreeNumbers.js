function smalestNumbers(a, b, c) {
    let smalest = (x, y, z) => { return Math.min(x, y, z) }
    console.log(smalest(a, b, c));
}

smalestNumbers(2,
    5,
    3
);
smalestNumbers(600,
    342,
    123
);