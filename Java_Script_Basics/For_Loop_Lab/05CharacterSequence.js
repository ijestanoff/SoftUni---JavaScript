function characterSequence (input) {
    let imp_string = input[0];
    let lengthString = imp_string.length;
    for (let char = 0; char < lengthString; char++)
        console.log(imp_string[char]);
}

// characterSequence (['Kolio']);